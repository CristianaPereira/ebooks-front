
const USERS = '/users';
const USERS_PARAMS = `${USERS}/:userId`;

const EBOOKS = '/ebooks';
const EBOOKS_PARAMS = `${EBOOKS}/:ebookId`;

export default {
  HOME: '/',
  LOGIN: '/signin',
  REGISTER: '/signup',
  USERS,
  USERS_PARAMS,
  EBOOKS,
  EBOOKS_PARAMS
}